## YAML Template.
---
spring:
    data:
        cassandra:
            keyspaceName: pocga
            contactpoints: localhost
            port: 9042
            username: admin
            password: password
        redis:
            host: localhost
            port: 6379
