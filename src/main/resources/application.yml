spring.profiles.active: dev
spring.cache.type: redis
spring.cache.redis.time-to-live: 60000

---

spring:
    profiles: dev
    data:
        cassandra:
            keyspacename: pocga
            contactpoints: localhost
            port: 9042
    redis:
        host: localhost
        port: 6379
pedidosga:
    urlApiProductos: https://fvwzxk56cg.execute-api.us-east-1.amazonaws.com/mock/productos/{idProducto}

---

spring:
    profiles: docker
    data:
        cassandra:
            keyspacename: pocga
            contactpoints: cassandra
            port: 9042
    redis:
        host: redis
        port: 6379
pedidosga:
    urlApiProductos: https://fvwzxk56cg.execute-api.us-east-1.amazonaws.com/mock/productos/{idProducto}

---

spring:
    profiles: prod
    data:
        cassandra:
            keyspacename: pocga
            contactpoints: cassandra
            port: 9042
    redis:
        host: redis
        port: 6379
pedidosga:
    urlApiProductos: http://catalogoga/catalogo/productos/{idProducto}
